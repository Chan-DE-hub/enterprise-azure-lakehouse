bundle:
  name: enterprise-azure-lakehouse

include:
  - resources/*.yml
  - resources/**/*.yml

artifacts:
  default:
    type: whl
    build: uv build --wheel
    path: .

targets:
  dev:
    mode: development
    default: true
    workspace:
      profile: DEFAULT
